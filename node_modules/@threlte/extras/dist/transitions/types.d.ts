import type { TransitionConfig } from 'svelte/transition';
import type { Simplify } from 'type-fest';
export declare type ThrelteTransitionInput<Ref> = [
    ref: Ref,
    options: {
        direction: 'in' | 'out' | 'both';
    }
];
export declare type ThrelteTransitionConfig = Simplify<Omit<TransitionConfig, 'css'>>;
export declare type ThrelteTransition<Ref> = (...args: ThrelteTransitionInput<Ref>) => ThrelteTransitionConfig;
